# PREFERRED: Use a YAML config file instead of env vars.
# See portal-config.testnet.yaml.example for the schema.
# Copy it to portal-config.yaml and set PORTAL_CONFIG if needed.
#
# Env vars below are supported as overrides — if both YAML and env var
# are set for a field, the env var wins.

# --- Override-only portal config (prefer YAML) ---
# ENABLE_ALLOWLIST=false
# ENABLE_BLOCKLIST=false
# LANDING_PAGE_OID_B36=1p3repujoigwcqrk0w4itsxm7hs7xjl4hwgt3t0szn6evad83q
# PORTAL_DOMAIN_NAME_LENGTH=21
# PREMIUM_RPC_URL_LIST=https://fullnode.testnet.sui.io|2|100
# RPC_URL_LIST=https://fullnode.testnet.sui.io|2|100
# SUINS_CLIENT_NETWORK=testnet
# AGGREGATOR_URL_LIST=https://aggregator.walrus-testnet.walrus.space|3|100
# SITE_PACKAGE=0xf99aee9f21493e1590e7e5a9aea6f343a1f381031a04a732724871fc294be799
# B36_DOMAIN_RESOLUTION_SUPPORT=true
# PROMETHEUS_EXPORTER_PORT=9184

# --- Secrets (env vars only, no YAML equivalent) ---
# BLOCKLIST_REDIS_URL=
# ALLOWLIST_REDIS_URL=
# EDGE_CONFIG=
# EDGE_CONFIG_ALLOWLIST=
