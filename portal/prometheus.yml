global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "pd-alert-rules.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "alertmanager:9093"

scrape_configs:
  - job_name: "walrus-sites"
    static_configs:
      - targets: ["localhost:9184"]
